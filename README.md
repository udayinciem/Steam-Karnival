# Steam-Karnival

## Environment Variables

Create a `.env` file in your project root with the following variable:

```
MONGO_URL=your_mongodb_connection_string
```

Replace `your_mongodb_connection_string` with your actual MongoDB URI. This variable will be used in scripts to connect to your MongoDB instance.